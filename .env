# CloudyWindow defaults (edit and export before running)

# Emoji used for app icon generation
CLOUDYW_EMOJI=🌦️

# Icon style: transparent (default) or glass
CLOUDYW_ICON_STYLE=transparent

# Output directory for generated icon (use a non‑iCloud path to avoid FS delays)
# Examples:
#   CLOUDYW_ICON_OUT=/tmp/cloudy-icons
#   CLOUDYW_ICON_OUT=./build
CLOUDYW_ICON_OUT=./build

# Window background alpha: 1 (near transparent, recommended) or 0 (fully transparent)
# 1 prevents afterimages without visible tint; 0 enables conditional flushes
CLOUDYW_WINDOW_ALPHA=1

# Disable accelerated 2D canvas in guest pages (1=true, 0=false). 1 is more stable but slower
CLOUDYW_CANVAS_SAFE_MODE=0

# Pre‑draw hard flush: hide webview before nav/opacity changes, show after dom‑ready (1=true, 0=false)
CLOUDYW_PREDRAW_FLUSH=0

